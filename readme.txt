Tell NPM grab these three package, and put them into the directory.
    // navigate to the working folder
    npm install --save gulp gulp-react gulp-concat

    // install globally cli
    npm install -g gulp-cli

