Install NPM

# initialize NPM. Press ENTER... x999
npm init

// Now it allows teammates to just do "npm install" and it'll install all the dependencies

# --save adds xxxx into our dependency in package.json
npm install xxxx --save

// Inside package.json you can add scripts so that you can run: "npm run xxxxxx"